export default {
  plugins: {
    // '@tailwindcss/postcss': {}, // Removed as Tailwind CSS is now via CDN
    // autoprefixer: {}, // Also remove autoprefixer as it might not be strictly necessary if not doing complex CSS transformations
  },
}
